---
import DeleteConfirmModal from "@/components/DeleteConfirmModal.astro";

export const partial = true;

const { folderId } = Astro.params;
---

<DeleteConfirmModal
  id={`${folderId}-delete`}
  confirmUrl={`/folder/${folderId}/delete`}
  confirmTarget="#folder-list"
/>
